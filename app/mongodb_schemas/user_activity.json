{
    "bsonType": "object",
    "required": ["sessionId", "userAction", "amount"],
    "properties": {
        "sessionId": {
            "bsonType": "objectId",
            "description": "Id of the project this user belongs to"
        },
        "userAction": {
            "bsonType": "string",
            "description": "The action performed by this user"
        },
        "timestamp": {
            "bsonType": "number",
            "description": "The unix timestamp (milliseconds) on which this action happened"
        },
        "amount": {
            "bsonType": "number",
            "description": "Magnitude of this event. For example, if this is a purchase event, the amount is the money spent on the purchase"
        }
    }
}
