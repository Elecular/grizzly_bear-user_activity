{
    "bsonType": "object",
    "required": ["projectId", "date", "count"],
    "properties": {
        "projectId": {
            "bsonType": "string",
            "description": "The action performed by this user"
        },
        "date": {
            "bsonType": "string",
            "description": "Date of when the MAU was calculated"
        },
        "count": {
            "bsonType": "number",
            "description": "Number of Monthly Active users recoded at this date"
        }
    }
}
