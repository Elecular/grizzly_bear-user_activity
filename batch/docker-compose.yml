  
version: '3'
services:
  spark-batch:
    depends_on:
      - user-activity-web
      - experiments-web
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
    - ./:/code/
    links:
    - "db:database"
    - "experiments-web:experiments-web"
    environment:
      MASTER: local
      MONGODB_URL: mongodb://username:password@database:27017
      MONGODB_DATABASE: experiments
      EXPERIMENTS_DEPLOYMENT_SERVICE_SERVICE_HOST: experiments-web
      EXPERIMENTS_DEPLOYMENT_SERVICE_SERVICE_PORT: 3000
    command: >
      bash -c "sbt local/reStart && tail -F anything"
  user-activity-web:
    depends_on:
      - db
    build:
      context: ../app
      dockerfile: Dockerfile.dev
    ports:
    - '80:80'
    volumes:
    - /code/node_modules
    links:
    - "db:database"
    environment:
      MONGODB_URL: mongodb://username:password@database:27017
      MONGODB_DATABASE: experiments
  experiments-web:
    depends_on:
      - db
    image: gcr.io/grizzly-bear-271511/grizzly-bear-stage/experiments
    ports:
    - '3000:80'
    - '8080:8080'
    volumes:
    - /code/node_modules
    links:
    - "db:database"
    environment:
      MONGODB_URL: mongodb://username:password@database:27017
      MONGODB_DATABASE: experiments
  db:
    image: mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: username
      MONGO_INITDB_ROOT_PASSWORD: password
